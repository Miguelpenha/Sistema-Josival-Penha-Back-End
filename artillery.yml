config:
  target: "http://localhost:3000/api"
  phases: 
    - duration: 600
      arrivalRate: 10
scenarios:
    - flow:
        - get:
            url: "/alunos"
            headers: 
              Authorization: "key {{ key }}"